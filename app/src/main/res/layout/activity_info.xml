<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:elevation="@dimen/appbar_elevation"
            android:theme="@style/ToolbarTheme"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>
    <RelativeLayout
            android:paddingTop="@dimen/avatar_padding_vertical"
            android:paddingBottom="@dimen/avatar_padding_vertical"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal">
        <ImageView
                android:id="@+id/avatar"
                android:layout_width="@dimen/chat_img_width"
                android:layout_height="@dimen/chat_img_height"
                android:layout_centerHorizontal="true"
                android:src="@drawable/ic_person"
                android:contentDescription="@string/avatar_hint"/>
        <Button
                android:id="@+id/logout"
                android:text="@string/logout_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/activity_padding_vertical"
                android:layout_marginBottom="@dimen/activity_padding_vertical"
                android:layout_below="@id/avatar"
                android:layout_centerHorizontal="true"
                android:visibility="gone"/>
        <TextView
                android:id="@+id/avatarPrompt"
                android:text="@string/avatar_prompt"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/activity_padding_vertical"
                android:layout_marginBottom="@dimen/activity_padding_vertical"
                android:layout_below="@id/avatar"
                android:layout_centerHorizontal="true"
                android:visibility="gone"/>
    </RelativeLayout>
</LinearLayout>